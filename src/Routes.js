import React from 'react';
import { Switch } from 'react-router-dom';

const Routes = ({user}) => {
    return (
        <Switch>
            ajaj ajsk ajsk asjk 
            {console.log(user)}
        </Switch>
    )
}

export default Routes;